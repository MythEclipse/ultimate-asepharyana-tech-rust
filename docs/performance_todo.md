# Performance Improvement TODO

- [x] Reduce per-request allocations in HTTP logging middleware.
- [x] Replace Redis `KEYS` scan in cache cleanup with incremental `SCAN`.
- [x] Validate the build and capture any follow-up issues.
